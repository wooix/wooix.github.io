# AI 연구 블로그 작업 원칙

이 문서는 이 저장소에서 글과 사이트를 편집하는 작업에 적용합니다.

## 중요한 용어는 English(한국어)로 반복합니다

사용자가 명시한 읽기·어휘 학습 요구가 최우선입니다. 상위 문서에 `한국어(English)` 또는 `첫 등장에만 병기`라는 지시가 있더라도, 이 프로젝트에서는 **English(한국어) 순서로 쓰고 중요한 재등장에서도 반복 병기**합니다.

- `Alignment(정렬)`, `Steering(행동 조절)`, `Quantization(양자화)`처럼 원래 영어 용어를 보존합니다.
- 약어를 처음 소개할 때는 `DPO(Direct Preference Optimization, 직접 선호 최적화)`처럼 풀어 씁니다. 이후 핵심 설명에서도 `DPO(직접 선호 최적화)`로 보조 설명을 유지합니다.
- 괄호로 끝나는 굵은 용어 뒤에 한국어 조사가 붙으면 CommonMark의 강조 구분자가 글자로 보일 수 있습니다. 글 본문의 강조는 `<strong>Alignment(정렬)</strong>는`처럼 명시적인 HTML 태그를 사용하고 미리보기에서 확인합니다.
- 영어를 모두 한국어로 대체하지 않습니다. 독자가 중요한 단어를 여러 문맥에서 다시 만날 수 있도록 씁니다.
- 짧게 합의한 탐색 메뉴, 코드, 서지정보의 원 제목, 용어 표의 영어 열은 원형을 유지할 수 있습니다.

## 글을 쓰는 방식

- 일상적인 비유로 진입하고 핵심 결론을 먼저 설명합니다. 짧은 문단과 `##`, `###` 제목을 사용합니다.
- 원 논문이 확인한 사실, 글쓴이의 해석, 아직 실행하지 않은 실험 제안을 구분합니다.
- 논문·저자 저장소·연구그룹 공식 발표 같은 Primary Source(1차 출처)를 직접 확인합니다. 주장 가까이에 링크를 둡니다.
- 논문 최초 공개일, 검토한 버전과 글 게시일을 구분합니다. 오래된 논문을 최신 뉴스처럼 소개하지 않습니다.
- 성능 수치에는 모델·평가·비교 조건이 필요합니다. 근거 없는 벤치마크나 실험 결과를 만들지 않습니다.
- FInject의 공개 자료와 비공개 연구 계획을 구분합니다. 공개 확인 없이 학회 채택·출판·후속 기여를 단정하지 않습니다.
- 기초 노트는 `milestone`, 관련 읽기·개념 노트는 `research-note`, 개별 논문 리뷰는 `research-review`로 분류합니다.

## 파일과 검증

- 글: `src/content/posts/*.md`. 필드 정의: `src/content.config.ts`.
- 탐색 분류: `src/data/topics.ts`. 관심사 운영 계획: `editorial.config.json`. 변경 시 두 곳을 함께 맞춥니다.
- 작업자가 여러 명이면 소유 범위를 확인하고 다른 작업자의 변경을 되돌리지 않습니다.
- 공개할 변경은 `npm run check`와 `npm run build`를 확인합니다. Pagefind 검색은 빌드 후 미리보기에서 확인합니다.
- 내부 세션 보고서, 검증 산출물, 응답 기록과 인증 정보는 공개 저장소에 포함하지 않습니다.

## Daily(일일) 편집 정책

`editorial.config.json`과 `docs/editorial-workflow.md`가 운영 계약입니다. 현재 `planned`는 설정과 문서만 존재함을 뜻합니다. 실행기·수신 채널·응답 조회·영속 상태를 연결하고 검증하기 전에는 자동화가 가동 중이라고 표현하지 않습니다.

07:00 요약을 제공하고, 사용자가 선택하면 해당 주제를 글로 작성합니다. 12:00 자동 선택은 **그날의 요약에 사용자 응답이 전혀 없을 때만** 하루 한 건에 적용합니다. 응답 여부를 확인할 수 없는 상태는 무응답으로 간주하지 않습니다. 날짜별 발행 기록으로 중복을 방지합니다.

## 합성데이터스터디 하위 논문 해설 (2026-09-14 사용자 지정)

이 시리즈의 하위 논문 요약은 위 일반 글쓰기 지침보다 다음 사용자 규칙을 우선한다. 전문 용어는 각 요약에서 처음 한 번만 English(한국어)로 병기하고 이후 English로 쓴다. 인사·임의의 도입·원문에 없는 절을 추가하지 않는다. 원문 절 순서·계층에 맞춰 요약·해석하고 핵심 수식은 의미와 설계 이유를 설명한다. 누락된 본문은 추측하지 않는다.

원고 규격과 게시 관계는 `docs/paper-summaries.md`를 따른다. 기존 강의 본문과 다른 일반 글의 용어 규칙은 유지한다.
